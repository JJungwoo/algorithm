/*
[BOJ] 1182. 부분수열의 합
*/

#include <iostream>
#include <algorithm>
#include <vector>
#include <tuple>
using namespace std;
#define io ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

int n, s;
int map[21];
bool visited[21];

void solve(int cnt)
{
    

}

int main()
{
    io;
    cin>>n>>s;
    for(int i=0;i<n;i++){
        cin>>map[i];
    }
    solve(0);
    return 0;
}