/*
online judge 12003. Array Transformer
cp-algorithm sqrt decomposition
u * k / (R - L + 1)
*/
#include <cstdio>
#include <vector>
#include <cmath>
using namespace std;
void init(){

}
int main()
{
    int n, m, u;
    scanf("%d %d %d", n, m, u);
    vector<int> a(n+1);
    for(int i=0;i<n;i++) scanf("%d", a[i]);   
    while(m--){
        int l,r,v,p;
        scanf("%d %d %d %d", &l, &r, &v, &p);

    }
    return 0;
}